class Intern{
    function intern(name, id,  email, school) {
    this.name = name;  
    this.id = id;
    this.email = email;    
    this.gitHub = gitHub;     
    }

    getRole(intern);
}

module.exports = intern;